# 학생 관리 시스템 - 프로젝트 설계 문서

## 목차
1. [프로젝트 개요](#1-프로젝트-개요)
2. [개발 환경](#2-개발-환경)
3. [기술 스택](#3-기술-스택)
4. [디렉토리 구조](#4-디렉토리-구조)
5. [데이터베이스 설계](#5-데이터베이스-설계)
6. [인증 구조](#6-인증-구조)
7. [데이터 흐름 및 컴포넌트 설계](#7-데이터-흐름-및-컴포넌트-설계)
8. [주요 설계 결정 및 이유](#8-주요-설계-결정-및-이유)
9. [환경 변수](#9-환경-변수)
10. [배포](#10-배포)

---

## 1. 프로젝트 개요

JEY 어학원의 수업 관리를 위한 내부 도구입니다.
출결, 숙제, 테스트 결과를 기록하고, 카카오 메시지를 통해 학부모에게 결과를 전달하는 것을 목적으로 합니다.

---

## 2. 개발 환경

| 항목 | 버전 |
|------|------|
| Node.js | v20 이상 |
| TypeScript | 5.x |
| Next.js | 14.2.x |
| React | 18.x |
| 패키지 매니저 | npm |

### 로컬 개발 시작

```bash
npm install
npm run dev     # http://localhost:3000
```

### 빌드 및 배포

```bash
npm run build   # 프로덕션 빌드
npm start       # 프로덕션 서버 실행
npm run lint    # ESLint 검사
```

---

## 3. 기술 스택

### 프레임워크 & 언어

| 기술 | 선택 이유 |
|------|-----------|
| **Next.js 14 (App Router)** | 서버 컴포넌트로 초기 데이터 로딩 최적화, 파일 기반 라우팅으로 구조가 단순함 |
| **TypeScript** | DB 스키마 타입을 코드에 반영해 런타임 오류 방지 |
| **React 18** | 최신 훅 기반 상태 관리, Next.js와 긴밀하게 통합 |

### 스타일링

| 기술 | 선택 이유 |
|------|-----------|
| **Tailwind CSS 3.4** | 클래스 기반으로 빠른 UI 개발, 별도 CSS 파일 없이 컴포넌트 내에서 완결됨 |
| **Toss 디자인 시스템** | 익숙하고 깔끔한 국내 서비스 디자인 언어 차용 (색상 토큰화) |
| **Pretendard 폰트** | 한글/영문 가독성이 좋고 학원 환경에 적합 |

### 백엔드 & 데이터베이스

| 기술 | 선택 이유 |
|------|-----------|
| **Supabase (PostgreSQL)** | 별도 API 서버 없이 클라이언트에서 직접 DB 조작 가능, 인증 내장, 스키마가 SQL로 명확하게 관리됨 |
| **@supabase/ssr** | Next.js App Router 환경에서 서버/클라이언트 양쪽 모두 동일한 Supabase 클라이언트 패턴 사용 가능 |

### 드래그 앤 드롭

| 기술 | 선택 이유 |
|------|-----------|
| **@dnd-kit** | React 친화적이고 접근성 지원, 가로(컬럼) + 세로(행) 두 방향 DnD를 독립적으로 구성할 수 있음 |

---

## 4. 디렉토리 구조

```
학생관리시스템/
├── src/
│   ├── app/
│   │   ├── layout.tsx                  # 루트 레이아웃 (폰트, 전역 스타일 적용)
│   │   ├── page.tsx                    # 메인 홈 (서버 컴포넌트 - 반 목록 fetch)
│   │   ├── globals.css                 # 전역 스타일
│   │   ├── login/
│   │   │   ├── page.tsx                # 로그인 페이지 (서버 컴포넌트)
│   │   │   └── actions.ts              # 로그인/로그아웃 서버 액션
│   │   ├── sessions/
│   │   │   └── [sessionId]/
│   │   │       ├── page.tsx            # 수업 상세 페이지 (서버 컴포넌트 - 전체 데이터 fetch)
│   │   │       ├── SessionView.tsx     # 수업 테이블 (클라이언트 컴포넌트 - 핵심 로직)
│   │   │       ├── ColumnModal.tsx     # 테스트 열 추가/수정 모달
│   │   │       └── AddStudentModal.tsx # 학생 추가 모달
│   │   └── _components/
│   │       ├── MainClient.tsx          # 홈 페이지 클라이언트 로직 (반/수업 CRUD)
│   │       ├── AddClassModal.tsx       # 반 추가 모달
│   │       ├── AddSessionModal.tsx     # 수업 추가 모달
│   │       ├── DeleteClassModal.tsx    # 반 삭제 확인 모달
│   │       ├── ManageStudentsModal.tsx # 학생 관리 모달
│   │       ├── BottomNav.tsx           # 하단 네비게이션 바
│   │       └── types.ts                # 공유 TypeScript 타입 정의
│   ├── lib/
│   │   └── supabase/
│   │       ├── client.ts               # 브라우저용 Supabase 클라이언트
│   │       └── server.ts               # 서버용 Supabase 클라이언트 (SSR)
│   └── middleware.ts                   # 인증 미들웨어 (페이지 보호)
├── supabase/
│   └── schema.sql                      # DB 스키마 정의
├── next.config.mjs
├── tailwind.config.ts
├── tsconfig.json
└── .env.local                          # 환경 변수 (git 미포함)
```

---

## 5. 데이터베이스 설계

### ERD (개념도)

```
classes
  └─< sessions
         └─< test_columns
         └─< student_records
               └─< test_results >─── test_columns
  └─< students
         └─< student_records
```

### 테이블 정의

#### `classes` — 반
| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID PK | |
| `name` | TEXT | 반 이름 (예: `HC1`, `MP3`) |
| `schedule` | TEXT | 수업 시간 (예: `월/수 19:00~20:30`) |
| `created_at` | TIMESTAMPTZ | |

#### `students` — 학생
| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID PK | |
| `class_id` | UUID FK | 소속 반 |
| `name` | TEXT | 학생 이름 |
| `note` | TEXT | 특이사항 (예: `ADHD`, `독해 약점`) |
| `order_num` | INTEGER | 화면 표시 순서 (DnD로 변경) |
| `kakao_chat_url` | TEXT | 카카오 채팅 URL (메시지 전송용) |

#### `sessions` — 수업 (날짜별)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID PK | |
| `class_id` | UUID FK | 소속 반 |
| `date` | DATE | 수업 날짜 |
| `time_range` | TEXT | 수업 시간대 (예: `18:00~20:00`) |
| `next_homework` | TEXT | 다음 수업 숙제 |

> UNIQUE(class_id, date) — 같은 반에 같은 날짜 수업 중복 방지

#### `test_columns` — 테스트 열
| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID PK | |
| `session_id` | UUID FK | 소속 수업 |
| `name` | TEXT | 열 이름 (예: `단어 TEST`) |
| `type` | TEXT | `word` / `review` / `custom` |
| `order_num` | INTEGER | 열 순서 (DnD로 변경) |

#### `student_records` — 수업별 학생 출결/숙제 기록
| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID PK | |
| `session_id` | UUID FK | 소속 수업 |
| `student_id` | UUID FK | 해당 학생 |
| `attendance` | TEXT | `present` / `absent` / `late` |
| `homework_status` | TEXT | `done` / `undone` |
| `note` | TEXT | 해당 수업 특이사항 메모 |

> UNIQUE(session_id, student_id) — 수업당 학생 기록 1개 보장

#### `test_results` — 테스트 결과
| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID PK | |
| `student_record_id` | UUID FK | 연결된 학생 기록 |
| `test_column_id` | UUID FK | 연결된 테스트 열 |
| `result` | TEXT | `P`, `X`, `P(74/80)`, `74/80`, `수요일` 등 자유 형식 |

> UNIQUE(student_record_id, test_column_id) — 학생×테스트 결과 1개 보장

### 설계 의도

- **`student_records`가 `test_results`의 부모 역할**: 출결/숙제와 테스트 결과를 분리하되 수업+학생 조합으로 묶음
- **`result`를 자유 텍스트로 저장**: P/X/점수/요일 등 다양한 형식을 DB 레벨에서 강제하지 않고 프론트에서 해석 → 이후 형식 확장이 유연함
- **`order_num`으로 정렬**: DB의 삽입 순서에 의존하지 않고 명시적으로 순서 관리

---

## 6. 인증 구조

### 흐름

```
브라우저 요청
    ↓
middleware.ts (모든 요청 인터셉트)
    ↓ supabase.auth.getUser()
    ├─ 비인증 → /login 리다이렉트
    └─ 인증 완료 → 통과
```

### 구현 방식

- **Supabase Auth** 이메일/비밀번호 방식 사용
- **쿠키 기반 세션**: `@supabase/ssr`이 서버/클라이언트 양쪽에서 세션 쿠키를 자동 관리
- **서버 액션** (`login/actions.ts`): 로그인/로그아웃은 서버에서 처리 후 리다이렉트
- **미들웨어**(`middleware.ts`): 모든 페이지 요청마다 세션 유효성 확인 및 갱신

### Supabase 클라이언트 두 종류

| 파일 | 용도 |
|------|------|
| `src/lib/supabase/server.ts` | 서버 컴포넌트, 서버 액션에서 사용 |
| `src/lib/supabase/client.ts` | 클라이언트 컴포넌트에서 사용 |

---

## 7. 데이터 흐름 및 컴포넌트 설계

### 서버 컴포넌트 vs 클라이언트 컴포넌트

```
page.tsx (서버 컴포넌트)
  → Supabase에서 전체 데이터 fetch (sessions, students, records, results, columns)
  → props로 클라이언트 컴포넌트에 전달
      ↓
SessionView.tsx (클라이언트 컴포넌트)
  → useState로 로컬 상태 관리
  → 사용자 인터랙션 시 Supabase 직접 호출 (update/upsert/delete)
  → Optimistic UI: 먼저 로컬 상태 업데이트 → 이후 DB 반영
```

**서버에서 초기 fetch를 하는 이유:** 페이지 진입 시 로딩 상태 없이 데이터가 즉시 표시됨. 클라이언트에서 fetch하면 빈 화면 → 로딩 스피너 → 데이터 표시 순서가 생겨 UX가 나빠짐.

**클라이언트에서 변경 시 직접 Supabase를 호출하는 이유:** 별도 API 라우트를 만들지 않아도 돼서 코드가 단순해짐. Supabase의 Row Level Security(RLS)로 보안은 DB 레벨에서 처리.

### Upsert 패턴

테스트 결과, 학생 기록 저장 시 `insert` 대신 `upsert` 사용:

```
결과 셀 클릭 → ensureRecord() → student_record가 없으면 생성
                              → test_results upsert (conflict 시 update)
```

이렇게 하면 "행이 있으면 업데이트, 없으면 생성" 로직을 단순하게 처리할 수 있음.

### 드래그 앤 드롭

`SessionView.tsx` 내에 두 개의 독립적인 DnD 컨텍스트가 중첩:

```
DndContext (세로 - 학생 행 순서)
  └─ DndContext (가로 - 테스트 열 순서)
```

드롭 완료 시 변경된 `order_num`을 DB에 일괄 업데이트.

### 카카오 메시지 생성

```
복사 버튼 클릭
  → generateMessage(student, records, columns, results)
      → formatResultForMessage(result) 로 각 결과를 한국어 텍스트로 변환
          "P"         → "완료"
          "P(74/80)"  → "완료(74/80)"
          "X"         → "미완료"
          "금"        → "미완료, 금요일 응시 예정입니다."
          "진행중, 금" → "진행중이며, 금요일 완료 예정입니다."
  → 클립보드에 복사
  → kakao_chat_url이 있으면 해당 URL 오픈
  → localStorage에 전송 완료 기록 (sent_{sessionId})
```

---

## 8. 주요 설계 결정 및 이유

### API 라우트 없이 클라이언트에서 Supabase 직접 호출

**이유:** 이 시스템은 단일 학원의 내부 도구로 복잡한 비즈니스 로직이 없음. 중간 API 레이어를 만드는 것은 오버엔지니어링이며, Supabase의 RLS(Row Level Security)로 보안을 충분히 처리할 수 있음.

### `result` 필드를 자유 텍스트로 저장

**이유:** 테스트 결과의 형식이 다양하고(P, X, 점수, 요일, 커스텀 텍스트) 이후에도 새로운 형식이 추가될 수 있음. DB 레벨에서 ENUM으로 강제하면 형식 추가마다 마이그레이션이 필요하지만, 자유 텍스트로 저장하면 프론트에서 해석 로직만 수정하면 됨.

### 하단 네비게이션 바를 localStorage에 저장

**이유:** 최근 방문한 수업 목록은 사용자의 브라우저 환경에 맞는 UI 상태이지, DB에 저장해야 할 비즈니스 데이터가 아님. localStorage로 처리하면 서버 호출 없이 즉시 복원됨.

### 전송 완료 여부를 localStorage에 저장

**이유:** 카카오 메시지 전송 여부는 해당 디바이스에서의 UI 피드백용이며 DB에 기록할 필요가 없음. 세션이 바뀌면 초기화되어야 하므로 `sent_{sessionId}` 키로 수업별 분리 저장.

### Toss 디자인 시스템 컬러 차용

**이유:** 별도 디자인 가이드 없이도 일관된 UI를 빠르게 만들기 위해 국내 사용자에게 익숙한 Toss의 컬러 팔레트를 참고. `tailwind.config.ts`에 토큰으로 정의해 코드 전체에서 일관되게 사용.

---

## 9. 환경 변수

`.env.local` 파일에 아래 두 가지 변수가 필요합니다:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

> `NEXT_PUBLIC_` 접두사로 브라우저에서도 접근 가능. Supabase의 anon key는 RLS가 활성화된 경우 공개해도 안전.

---

## 10. 배포

현재 **Vercel**에 배포되어 운영 중입니다.

- Vercel에서 GitHub 레포와 연결 후 자동 배포
- 환경 변수는 Vercel 프로젝트 설정에서 주입
- Next.js App Router와 Vercel의 Edge 런타임이 최적으로 통합됨
